<UserControl x:Class="HITAPEX.Views.HelpUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:lex="clr-namespace:HITAPEX.Helpers"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="40,30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,30">
                <TextBlock Text="{lex:Loc Help.Title}" FontSize="32" FontWeight="Bold" Foreground="#FFFFFF"/>
                <TextBlock Text="{lex:Loc Help.Subtitle}" FontSize="16" Foreground="#8892A0" Margin="0,8,0,0"/>
            </StackPanel>

            <Border Grid.Row="1" Background="#1E2A3A" CornerRadius="12" Padding="24,20" Margin="0,0,0,24">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="{lex:Loc Help.SearchPlaceholder}" FontSize="16" Foreground="#8892A0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Border Grid.Column="1" Background="#00D4FF" CornerRadius="6" Padding="16,8" Cursor="Hand">
                        <TextBlock Text="{lex:Loc Help.Search}" Foreground="#0D1B2A" FontWeight="SemiBold"/>
                    </Border>
                </Grid>
            </Border>

            <StackPanel Grid.Row="2">
                <TextBlock Text="{lex:Loc Help.FAQ}" FontSize="18" FontWeight="SemiBold" Foreground="#FFFFFF" Margin="0,0,0,12"/>

                <Border Background="#1E2A3A" CornerRadius="12" Padding="20,16" Margin="0,0,0,8" Cursor="Hand">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{lex:Loc Help.FaqConnect}" Foreground="#FFFFFF" FontSize="14" VerticalAlignment="Center"/>
                        <TextBlock Text="›" Grid.Column="1" Foreground="#8892A0" FontSize="18"/>
                    </Grid>
                </Border>

                <Border Background="#1E2A3A" CornerRadius="12" Padding="20,16" Margin="0,0,0,8" Cursor="Hand">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{lex:Loc Help.FaqCalibrate}" Foreground="#FFFFFF" FontSize="14" VerticalAlignment="Center"/>
                        <TextBlock Text="›" Grid.Column="1" Foreground="#8892A0" FontSize="18"/>
                    </Grid>
                </Border>

                <Border Background="#1E2A3A" CornerRadius="12" Padding="20,16" Margin="0,0,0,8" Cursor="Hand">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{lex:Loc Help.FaqFFB}" Foreground="#FFFFFF" FontSize="14" VerticalAlignment="Center"/>
                        <TextBlock Text="›" Grid.Column="1" Foreground="#8892A0" FontSize="18"/>
                    </Grid>
                </Border>

                <Border Background="#1E2A3A" CornerRadius="12" Padding="20,16" Margin="0,0,0,8" Cursor="Hand">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{lex:Loc Help.FaqGraphics}" Foreground="#FFFFFF" FontSize="14" VerticalAlignment="Center"/>
                        <TextBlock Text="›" Grid.Column="1" Foreground="#8892A0" FontSize="18"/>
                    </Grid>
                </Border>

                <Border Background="#1E2A3A" CornerRadius="12" Padding="20,16" Cursor="Hand">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{lex:Loc Help.FaqMultiplayer}" Foreground="#FFFFFF" FontSize="14" VerticalAlignment="Center"/>
                        <TextBlock Text="›" Grid.Column="1" Foreground="#8892A0" FontSize="18"/>
                    </Grid>
                </Border>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
