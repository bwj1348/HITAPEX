<UserControl x:Class="HITAPEX.Views.HelpUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="40,30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,30">
                <TextBlock Text="帮助中心" FontSize="32" FontWeight="Bold" Foreground="#FFFFFF"/>
                <TextBlock Text="查找教程和常见问题解答" FontSize="16" Foreground="#8892A0" Margin="0,8,0,0"/>
            </StackPanel>

            <Border Grid.Row="1" Background="#1E2A3A" CornerRadius="12" Padding="24,20" Margin="0,0,0,24">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="🔍 搜索帮助文档" FontSize="16" Foreground="#8892A0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Border Grid.Column="1" Background="#00D4FF" CornerRadius="6" Padding="16,8" Cursor="Hand">
                        <TextBlock Text="搜索" Foreground="#0D1B2A" FontWeight="SemiBold"/>
                    </Border>
                </Grid>
            </Border>

            <StackPanel Grid.Row="2">
                <TextBlock Text="常见问题" FontSize="18" FontWeight="SemiBold" Foreground="#FFFFFF" Margin="0,0,0,12"/>

                <Border Background="#1E2A3A" CornerRadius="12" Padding="20,16" Margin="0,0,0,8" Cursor="Hand">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="如何连接方向盘设备？" Foreground="#FFFFFF" FontSize="14" VerticalAlignment="Center"/>
                        <TextBlock Text="›" Grid.Column="1" Foreground="#8892A0" FontSize="18"/>
                    </Grid>
                </Border>

                <Border Background="#1E2A3A" CornerRadius="12" Padding="20,16" Margin="0,0,0,8" Cursor="Hand">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="如何校准踏板灵敏度？" Foreground="#FFFFFF" FontSize="14" VerticalAlignment="Center"/>
                        <TextBlock Text="›" Grid.Column="1" Foreground="#8892A0" FontSize="18"/>
                    </Grid>
                </Border>

                <Border Background="#1E2A3A" CornerRadius="12" Padding="20,16" Margin="0,0,0,8" Cursor="Hand">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="力反馈设置建议" Foreground="#FFFFFF" FontSize="14" VerticalAlignment="Center"/>
                        <TextBlock Text="›" Grid.Column="1" Foreground="#8892A0" FontSize="18"/>
                    </Grid>
                </Border>

                <Border Background="#1E2A3A" CornerRadius="12" Padding="20,16" Margin="0,0,0,8" Cursor="Hand">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="如何调整画面设置以获得最佳性能？" Foreground="#FFFFFF" FontSize="14" VerticalAlignment="Center"/>
                        <TextBlock Text="›" Grid.Column="1" Foreground="#8892A0" FontSize="18"/>
                    </Grid>
                </Border>

                <Border Background="#1E2A3A" CornerRadius="12" Padding="20,16" Cursor="Hand">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="多人联机游戏指南" Foreground="#FFFFFF" FontSize="14" VerticalAlignment="Center"/>
                        <TextBlock Text="›" Grid.Column="1" Foreground="#8892A0" FontSize="18"/>
                    </Grid>
                </Border>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
